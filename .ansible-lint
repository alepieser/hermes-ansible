---
# Ansible Lint configuration
# ==========================

# Skip certain rules that are not critical for CI
skip_list:
  - yaml[line-length]  # Allow longer lines for readability
  - jinja[spacing]     # Allow some Jinja2 spacing variations

# Use a more lenient profile
profile: min

# Exclude certain files from linting
exclude_paths:
  - group_vars/all.vault.yml  # Vault files are encrypted
  - .ansible-lint
  - .yamllint
  - roles/*/molecule/  # Exclude molecule test files
  - roles/alepieser.*/  # Exclude custom alepieser roles